# 个人灵感库：V1 产品定义

## 1. 一句话定义

一个在浏览器内完成收藏、自动整理和重新查找的个人网络灵感库插件。

它服务于设计和开发工作：用户通过链接添加网站、文章或关注源，系统从名称、描述、封面、内容类型和标签中建立识别线索，帮助用户在一分钟内找回可用于当前需求的内容。

## 2. 产品原则

1. **先添加，后整理**：添加只要求选择内容类型并提供链接，不要求用户先整理标签。
2. **可识别比信息齐全更重要**：卡片先帮用户快速认出网站，再提供详细信息。
3. **类型稳定，标签灵活**：网站、文章和关注源是固定内容类型；主题、风格、技术和用途由可编辑标签表达。
4. **标签负责具体语义**：主题、风格、技术、能力和使用场景都用标签表达。
5. **AI 是助手，不是门卫**：AI 失败不能阻止收藏，所有 AI 结果均可纠正。
6. **本地优先**：V1 单人、单机使用，数据保存在浏览器中，不引入账户和云同步。

## 3. V1 用户故事

### 添加

- 我可以从内容区底部中央的添加按钮选择“网站”“文章”或“关注源”。
- 三种内容类型均通过链接添加，选择的类型决定卡片的展示结构。
- 我可以在添加时填写一段可选描述，并输入、新增或选择标签，也可以只提交链接。
- 手动添加时，我可以选择临时授权当前网站以补全标题、描述、favicon 和公开封面；拒绝授权或补全失败不影响收藏保存。
- 我填写的描述直接用于卡片和紧凑卡片；未填写时，系统依次使用网页自带描述或 AI 总结。列表为快速扫描不显示描述，但详情仍保留完整内容。
- 即使网页抓取或 AI 处理失败，链接也必须先保存成功。
- 重复添加相同内容类型和规范化 URL 时不创建重复卡片。

### 浏览与管理

- 我可以在浏览器的完整页面中打开灵感库。
- 我可以从左侧切换全部、未处理、星标、网站、文章和关注源。
- 我可以在侧边栏一次恢复一个快捷视图。
- 我可以添加、修改、合并或删除标签。
- 我可以对少量卡片添加或移除星标，便于当前工作中快速找回。
- 我可以查看网页的本地正文快照。

### 查找

- 我可以搜索名称、描述、域名、标签和快照正文。
- 我可以在当前侧栏范围内多选标签，并看到筛选结果数量。
- 我可以按最新或最旧排列筛选结果。
- 点击内容卡片打开侧边详情；仅点击网址打开原网页。

### 导入

- 我可以导入浏览器导出的书签 HTML。
- 我可以一次粘贴多个 URL 批量收藏。
- 我可以选择一次性临时授权本批次涉及的网站，以补全标题、描述、favicon 和公开 OG 封面；拒绝授权不影响链接导入。
- 导入项先保存，再在后台逐步生成描述和标签。

## 4. 信息架构

### 顶部导航

- 产品名称或标识
- 全局搜索框

V1 没有账户系统，因此右上角不需要用户头像作为核心入口。

### 左侧导航

- 收藏库：全部、未处理、星标。
- 内容类型：网站、文章、关注源。
- 快捷视图：由用户保存的常用筛选条件。
- 底部只保留“设置”。导入收藏夹、外观、AI 与后续的日报周刊统一从设置进入。

### 添加入口

- 固定在主内容区底部中央，不占用侧边栏。
- 默认显示一个圆形主按钮，悬停、键盘聚焦或点击时向上展开网站、文章和关注源三个动作。
- 动效可参考液态融合，但图标、文字、焦点和点击目标必须保持清晰。

### 右侧内容区

1. 当前视图名称及数量。
2. 左侧显示当前范围及结果数，并提供可搜索、可多选的“标签”入口；右侧提供“最新 / 最旧”排序和“卡片 / 紧凑卡片 / 列表”排列按钮。
3. 已选标签显示在工具栏下一行，可逐个移除；根据可用宽度只展示部分标签，其余通过展开按钮查看。筛选结果区同时提供结果数、“保存为快捷视图”和“清除全部”。
4. 主体支持卡片、紧凑卡片和列表三种排列；三种排列都根据网站、文章和关注源呈现不同的内容结构。紧凑卡片根据内容区宽度显示两列或一列瀑布流；列表始终为单列、自适应宽度，并使用分割线区分项目。
5. 侧边详情面板，不离开当前搜索上下文。

## 5. 灵感卡片

### 卡片直接显示

- 封面
- 名称
- 域名或来源名称
- 1–2 行描述
- 3–5 个核心标签
- 星标状态

卡片和紧凑卡片不设置备注区。描述只占一个固定信息位置：用户填写的描述优先；没有用户描述时显示网页自带描述；网页没有可用描述时才显示 AI 总结。不同来源的描述不重复并列展示；列表按照 Figma 组件仅展示来源、标题和标签。

### 封面选择规则

1. 高质量 Open Graph 图。
2. 没有 Open Graph 图时使用设计系统占位封面，不把站点 Logo 或 favicon 拉伸为封面。
3. favicon 仅作为站点图标使用。
4. 用户手动更换后，不再自动覆盖。

### 侧边详情显示

- 完整描述
- 内容类型、星标与全部标签
- 正文快照及快照完整性
- 原始 URL、收藏时间、来源站点
- 编辑、删除和打开原文操作
- 封面图：网站始终显示封面区；文章收藏卡片不显示封面，但详情中有 Open Graph 图时显示、没有时不渲染封面区；关注源保持原有封面形式

详情中的标题、描述、标签和封面在当前面板原位编辑，不进入独立编辑页面。

“未处理”是仅用于筛选的整理状态：收藏项同时没有用户描述且没有标签时为未处理；用户补充其中任意一项后即视为已处理。已处理与未处理不在内容卡片或详情中显示状态标识。

## 6. AI 整理规则

AI 使用用户自行配置的 API Key，负责：

- 清理或补全网页名称。
- 生成一段用于卡片识别的简短描述。
- 生成主题、风格、技术、能力和场景标签。
- 识别封面候选图。

AI 生成的描述和标签跟随用户选择的界面语言，不随网页原文语言切换；品牌、产品和技术等必要专有名词可保留原文。V1 尚未提供语言切换界面，默认语言为简体中文，但语言设置和 AI 输出接口必须预留，后续增加语言选项时不改动 Provider 或任务流程。

描述生成遵循：

1. 用户填写的描述不可被网页采集或 AI 覆盖。
2. 未填写时优先使用网页自带描述。
3. 仅当用户描述和网页描述都不存在或不可用时，才生成 AI 描述。
4. 卡片和紧凑卡片始终读取最终描述，不因来源增加额外字段或展示区块；列表不渲染描述。

标签生成必须遵守：

1. 优先复用已有标签。
2. 合并近义词，避免重复概念。
3. 默认展示 3–5 个核心标签。
4. 用户的修改优先级高于后续 AI 生成。
5. API 不可用时，项目进入“待处理”，可手动重试。

V1 的搜索是本地关键词与全文检索，不实现 AI 语义搜索。

## 7. 本地数据模型

### SavedItem

- `id`
- `originalUrl`
- `canonicalUrl`
- `siteHost`
- `title`
- `description`
- `descriptionSource`（`user`、`page` 或 `ai`）
- `cover`
- `kind`
- `tagIds[]`
- `isFavorite`（星标状态）
- `snapshotId`
- `snapshotStatus`
- `aiStatus`
- `createdAt`
- `updatedAt`
- `lastOpenedAt`

### Snapshot

- `id`
- `itemId`
- `capturedAt`
- `title`
- `byline`
- `excerpt`
- `cleanText`
- `cleanHtml`
- `captureMethod`
- `completeness`

### Tag

- `id`
- `name`
- `normalizedName`
- `aliases[]`
- `usageCount`

### SavedView

- `id`
- `name`
- `scope`（全部、未处理、星标或一种内容类型）
- `tagIds[]`

快捷视图只保存内容范围与标签组合，不保存搜索词、结果排序或排列样式。调用快捷视图时，排序使用默认的“最新”。

## 8. 浏览器内的处理流程

1. 立即将 URL、页面标题、域名和收藏时间写入本地数据库。
2. 用户从当前标签页主动添加时，扩展使用临时授予的页面权限读取已渲染 DOM 和元数据。
3. 手工输入 URL 时可选择对该域名单次授权；批量链接与书签导入可选择一次授权本批次的精确域名集合。两者都只补全公开元数据并在结束后立即撤销，默认关闭且不申请全站访问权限。
4. 生成正文快照，并明确标记完整、部分或失败。
5. 若用户填写了描述，直接保存并锁定其优先级；否则采用网页自带描述。
6. 选择封面候选图。
7. 在用户已配置 API Key 时，为仍缺少描述的收藏项生成描述，并异步生成标签；用户描述和内容类型不被 AI 覆盖。
8. 刷新对应卡片，不阻断用户继续浏览。

## 9. V1 必做范围

- Chrome/Edge 兼容的浏览器扩展。
- 网站、文章和关注源三种链接添加入口。
- 扩展内的完整页管理界面。
- 根据内容类型变化结构的小型网格卡片。
- 可编辑标签与可编辑快捷视图。
- 卡片星标与侧边栏星标视图。
- AI 生成的描述和标签，可人工修改。
- 本地关键词搜索与多标签筛选。
- 保存一次收藏时的正文快照。
- 相同 URL 去重，同站点页面可聚合查看。
- 书签 HTML 导入与批量 URL 导入。

## 10. V1 明确不做

- 网页变化检测、版本历史和差异对比。
- 日报、周刊、邮件和对外发布。
- AI 语义搜索和自然语言需求检索。
- 移动端、账户系统、云同步和多人协作。
- 项目灵感板、设计对比和自动生成代码提示词。
- 公开社区、分享页和推荐关系。
- 绕过登录、付费墙或网站访问限制。
- 归档、30 天回收站和完整数据导出。

## 11. 验收标准

1. 从看到网页到完成添加，默认不需要填写任何字段。
2. 添加操作完成后，新卡片立即可见；AI 处理可后续完成。
3. 用户可从卡片识别网站，并在两次操作内打开原页面。
4. 添加或移除星标后，侧边栏星标视图立即更新。
5. 未配置 API Key 或 API 失败时，收藏、编辑、搜索和打开链接仍可正常使用。
6. 搜索可覆盖名称、描述、域名、标签和快照正文。
7. 书签 HTML 和批量 URL 导入不产生相同规范化 URL 的重复卡片。
8. 从任一侧栏范围切换到另一范围后，临时标签筛选被清空，排序恢复为“最新”。
9. 用户可在卡片、紧凑卡片与列表排列之间即时切换；紧凑卡片按可用宽度呈现两列或一列瀑布流，列表保持单列并按内容类型分别呈现网站 OG 封面、文章标题和关注源头像，且不改变当前搜索、筛选与排序状态。
10. 未星标卡片的星标按钮只在卡片悬停或键盘聚焦时出现；点击后按钮常驻并可再次点击取消。
11. 添加时填写描述后，卡片和紧凑卡片显示该描述；未填写时按“网页描述 → AI 总结”的顺序补全，且不出现备注字段或第二段说明；列表保持不显示描述。

## 12. 成功指标

V1 不以收藏数量为成功。核心验证是：

- 面对一个设计或开发需求时，能否在一分钟内找到合适的网站。
- 找到的内容是否真正被用于项目。
- 收藏时是否因需要整理而放弃。
- AI 生成的描述和标签需要被纠正的比例是否逐步下降。

## 13. 后续路线图的触发条件

- **变化检测**：只有当用户经常因原网页改版或失效而受损时才开发。
- **日报/周刊**：只有当收藏量足以形成稳定内容流时才开发。周刊以新收藏为主并搭配历史内容；日报至少有 3 条候选且能形成明确主题。
- **语义搜索**：只有当关键词和筛选经常无法命中已知存在的内容时才开发。
- **云同步与移动收藏**：只有当单机版被持续使用，且跨设备缺失成为明确痛点时才开发。
- **存档管理**：验证基础收藏与查找后，再增加归档、30 天回收站和可恢复快照的完整数据导出。
