# Wanderland 产品开发路线图

> 更新日期：2026-09-09  
> 当前版本：`v0.6.2`
> 当前阶段：元数据补全与三种内容展示模式完成，进入 V1 发布验证

## 1. 路线图目标

这份文档负责回答三个问题：项目现在能做什么、距离可用的 V1 还缺什么、接下来按什么顺序交付。

产品范围与验收标准以 `PRODUCT.md` 为准，视觉与交互以 `DESIGN.md` 为准，技术边界以 `TECH-STACK.md` 和已接受 ADR 为准。本路线图只负责任务排序、阶段出口和发布判断，不重复定义产品。

V1 的目标不是堆积功能，而是打通一个可靠的本地闭环：

1. 用户主动添加当前网页或一个链接。
2. 收藏项立即保存，关闭或重启浏览器后仍然存在。
3. 系统在后台补全元数据、正文快照、封面、描述和标签；任何失败都不阻断收藏。
4. 用户可以通过范围、标签和本地全文搜索，在一分钟内重新找到内容。
5. 用户可以纠正系统结果，且人工修改不会被后续自动处理覆盖。

## 2. 当前进度基线

### 已完成

- WXT、React、TypeScript 和 Chrome Manifest V3 工程骨架。
- 扩展按钮打开完整 Dashboard，权限保持为 `activeTab`、`scripting` 和 `storage`。
- Wanderland V0.2 设计系统、Display-P3 token、共享 UI 原语和开发预览页。
- 网站、文章、关注源三种固定内容类型及其卡片、紧凑卡片、列表展示。
- 全部、未处理、星标、内容类型和快捷视图侧栏。
- 标签组合筛选、简单关键词过滤、最新/最旧和卡片/紧凑卡片/列表切换。
- 星标交互，快捷视图创建、重命名、删除和排序交互。
- 链接添加、可选描述、批量 URL 和书签 HTML 导入界面。
- 详情灯箱、键盘前后切换、规范化 URL 和描述来源优先级基础逻辑。
- Dexie schema、v1→v3 迁移、repository 与 `useLiveQuery()` 响应式查询。
- 添加、批量导入、星标与快捷视图的本地持久化，以及内容类型 + 规范化 URL 事务去重。
- 按真实时间字段排序，演示数据仅在开发环境通过 `?seed=demo` 显式启用。
- 当前页采集、Readability 正文提取、DOMPurify 清理、正文快照与失败重试。
- MiniSearch 全文检索、标签管理、收藏项编辑删除、站点组、快照阅读与图片浏览。
- 可替换 AI Provider、OpenAI/DeepSeek/自定义 OpenAI-compatible 接入、Zod 结构化校验与描述/标签合并规则。
- AI 设置、会话/本地 Key 保存、Provider 域名按需授权、持久化任务恢复与人工重试。
- `pnpm check` 当前通过，包括类型检查、单元测试和 Chrome MV3 生产构建。

### 当前限制

- 还没有使用 Playwright 持久化 Chromium Context 的真实 MV3 端到端测试。
- 还未完成 Chrome/Edge 安装、权限、升级、可访问性与真实数据连续试用验收。
- AI Provider 的实际兼容性依赖用户配置的 Endpoint 是否实现 OpenAI-compatible Chat Completions 协议。

### 进度判断

| 维度 | 状态 | 判断 |
| --- | --- | --- |
| 产品与领域边界 | 稳定 | V1 范围、术语和非目标已明确 |
| 视觉与主要交互 | 接近完成 | 足以支撑后续功能开发和用户测试 |
| 本地数据闭环 | 完成 | IndexedDB 是唯一业务数据源，核心写操作可持久化 |
| 网页采集与快照 | 完成 | 当前页采集、清理、快照与恢复已接入 |
| 整理与查找 | 完成 | 真实索引、标签管理和上下文恢复已完成 |
| AI 整理 | 完成 | 可选 Provider、人工优先合并与任务恢复已接入 |
| 发布验证 | 未开始 | 缺少真实扩展 E2E 和浏览器验收 |

因此，`v0.6.2` 已完成 V1 功能链路、临时授权元数据补全与三种展示模式收尾，但在真实 MV3 端到端测试与 Chrome/Edge 发布验证完成前仍不视为 V1 发布版。

## 3. 执行原则

1. **先纵向闭环，再横向扩展**：优先让一个真实收藏项能够保存、重启恢复、采集和搜索，再增加更多界面能力。
2. **先本地能力，再 AI**：未配置 AI 时，添加、编辑、搜索、快照和打开来源必须完整可用。
3. **先持久化任务，再执行后台处理**：Service Worker 随时可能停止，采集和 AI 状态不得只放在内存中。
4. **人工数据优先**：用户描述、用户标签、固定内容类型和手动封面不得被自动结果覆盖。
5. **索引可以重建，业务数据不能依赖索引**：Dexie/IndexedDB 是唯一业务数据源，MiniSearch 只是派生索引。
6. **保持最小权限**：当前页添加优先读取用户主动授权的已渲染 DOM；手工 URL 只请求单域名临时权限，批量导入只在用户显式选择后一次请求本批次的精确域名集合；补全公开元数据后立即撤销，不为自动抓取申请全站权限。
7. **每个版本都有可观察出口**：版本完成以用户可验证的行为为准，而不是以文件或组件数量为准。

## 4. 版本路线图

### v0.3 — Local Core ✅

**目标**：把现有交互原型替换为真实、可迁移、可响应更新的本地业务数据。

**完成日期**：2026-09-08。

主要交付：

- 补齐 `SavedItem`、`Snapshot`、`Tag`、`SavedView` 和持久化任务领域模型。
- 在 `src/db/` 建立 Dexie schema、版本迁移、repository 和查询接口。
- 对规范化 URL 与内容类型建立数据库级唯一约束或等价的事务去重策略。
- 使用 `useLiveQuery()` 将 Dashboard 从演示数组迁移到 repository。
- 持久化添加、批量导入、星标和快捷视图的创建、修改、删除与排序。
- 使用真实时间字段排序，移除依靠数组顺序表达“最新/最旧”的逻辑。
- 将演示数据降级为仅开发环境显式启用的种子数据。
- 覆盖 repository、去重和数据库迁移单测。

阶段出口：

- 添加收藏项后刷新 Dashboard、关闭扩展页或重启浏览器，数据仍然存在。
- 重复添加相同内容类型和规范化 URL 时只保留一个收藏项。
- 星标与快捷视图状态在重启后保持一致。
- 数据库升级不会丢失已有收藏项。

### v0.4 — Capture & Snapshot ✅

**目标**：打通用户当前正在浏览的网页到本地收藏项和正文快照的完整链路。

**完成日期**：2026-09-08。

主要交付：

- 增加按需注入的当前页快速收藏浮层，并复用 Dashboard 的添加用例。
- Background 负责消息协调和持久化任务推进，不保存关键内存队列。
- 使用 `activeTab + scripting` 读取当前页的标题、描述、canonical URL、Open Graph、favicon 和已渲染 DOM。
- 将克隆后的 Document 交给 Readability，并使用 DOMPurify 清理保存的 HTML。
- 保存正文文本、清理后的 HTML、采集方式、完整性和错误信息。
- 仅使用页面提供的 Open Graph 图作为自动封面；缺失时显示占位封面，favicon 只作为站点图标。
- 手工 URL 与批量导入项立即保存并标记待补全；没有权限时不伪装采集成功。
- 实现失败任务恢复和人工重试的基础机制。

阶段出口：

- 用户在真实文章页主动添加后，新收藏项立即出现在 Dashboard。
- 关闭并重新打开扩展后，收藏项及正文快照仍可读取。
- SPA、登录页或采集失败场景至少保留 URL、类型、时间和明确状态。
- 用户填写的描述不会被网页描述或后续处理覆盖。

### v0.5 — Organize & Find ✅

**目标**：让真实收藏数据可维护，并验证“一分钟内找回内容”的核心价值。

**完成日期**：2026-09-08。

主要交付：

- 在 `src/search/` 建立 MiniSearch 索引和 `Intl.Segmenter` 中英文分词。
- 搜索名称、描述、域名、标签和正文快照，并保留合理的字段权重。
- 完成标签新增、修改、合并、删除、别名和使用计数。
- 完成收藏项标题、描述、标签、星标、删除和手动封面替换。
- 完成详情中的正文快照阅读，以及同一受控 Modal 内的图片浏览状态机。
- 用 URL search params 恢复搜索、范围、快捷视图、标签筛选和当前详情项。
- 恢复关闭详情前的滚动位置和触发元素焦点。
- 实现同站点多个收藏项的站点组浏览。
- 针对大量真实收藏项检查瀑布流性能、图片加载和索引重建时间。

阶段出口：

- 搜索可以命中只存在于正文快照中的词。
- 标签与快捷视图修改在重启后保持一致。
- 从详情返回后，搜索、筛选、排序、排列和滚动上下文不丢失。
- 使用一批真实收藏数据进行人工任务测试时，目标内容可在一分钟内找到。

### v0.6 — AI Assistant ✅

**目标**：在不改变本地优先边界的前提下，用可选 AI 降低整理成本。

**完成日期**：2026-09-09。

主要交付：

- 在 `src/ai/` 定义可替换 `AiProvider` 接口。
- 实现 OpenAI-compatible HTTP Provider，以及 Endpoint、Model 和 API Key 配置。
- 使用 Zod 校验描述和标签的结构化输出。
- 按“用户描述 → 网页描述 → AI 描述”的优先级补全描述。
- 优先复用已有标签，保留人工修改，避免近义词和重复标签扩散。
- 保存任务状态、错误原因、尝试次数和可恢复重试信息。
- 提供人工重试、禁用 AI 和仅当前会话保存 Key 的选项。
- 在设置中明确 API Key 的本地存储边界和发送给 Provider 的内容范围。

阶段出口：

- 完全未配置 AI 时，所有核心功能仍然通过验收。
- AI 返回无效 JSON、超时或拒绝请求时，不丢失收藏项，也不覆盖人工数据。
- 重启浏览器后，未完成任务可以恢复或由用户重试。

### v0.6.1 — AI Settings & UI Polish ✅

**目标**：收紧组件库使用规则，完善 AI 设置的安全验证与交互一致性。

**完成日期**：2026-09-09。

主要交付：

- 建立 Intent UI 优先的组件复用规则，并将 Modal、Menu、Switch、Radio Group、Search Field 与 Segmented Control 收敛到共享 UI 层。
- 将侧栏设置入口改为锚定菜单，修正菜单悬停残留、显示样式切换和表单背景状态。
- 为长设置界面固定标题栏与底部操作栏，并统一应用于 AI、添加和导入流程。
- 增加 AI Provider 联通测试；临时 Key 只用于当前请求，不因测试写入持久化存储，错误信息会执行 Key 脱敏。
- AI 描述与标签跟随应用语言生成，当前默认简体中文，并保留未来语言切换入口。
- 补充组件规范、AI 语言和连接测试相关文档与自动化测试。

阶段出口：

- 设置菜单与基础控件在 hover、focus-visible、selected 和 disabled 状态下表现一致。
- AI Key 可在保存前验证联通，失败信息不会回显完整 Key。
- 应用语言为简体中文时，AI 输出明确要求使用简体中文。
- `pnpm check` 通过，提交内容不包含真实 API Key 或构建产物。

### v0.6.2 — Metadata Enrichment & Library Layouts ✅

**目标**：在最小权限边界内补全远程收藏元数据，并完善收藏库的信息密度切换。

**完成日期**：2026-09-09。

主要交付：

- 手动 URL、批量 URL 与书签 HTML 导入可显式请求精确站点的临时权限，读取公开标题、描述、favicon 与 Open Graph 封面后立即撤销。
- 远程采集不携带 Cookie 与 Referer，并设置超时、响应大小、内容类型和消息来源校验；授权拒绝或采集失败不影响本地保存。
- favicon 增加“页面声明 → 根目录 `/favicon.ico` → 本地图标”回退链路，封面继续只使用真实 Open Graph 图片或设计系统占位。
- 展示模式扩展为“详情卡片 / 紧凑卡片 / 详情列表”：紧凑卡片支持两列或一列瀑布流，新列表按 Figma 组件实现单列、自适应宽度与分割线。
- 列表补齐网站封面、关注源头像、星标位置、来源链接悬停状态及三种展示模式的 Tooltip。
- 更新产品、设计、技术、权限与 ADR 文档，并补充远程元数据、favicon 和导入结果测试。

阶段出口：

- 不持有 `<all_urls>`，所有新增站点权限均由用户显式触发，并在成功或失败后撤销。
- 三种显示样式切换不改变当前搜索、筛选和排序上下文。
- `pnpm check` 通过，提交内容不包含真实 API Key、浏览数据或构建产物。

### v1.0 — Validation Release

**目标**：达到 `PRODUCT.md` 中 V1 必做范围和验收标准，可用于真实日常收藏。

主要交付：

- 使用 Playwright 持久化 Chromium Context 加载真实 MV3 扩展。
- 覆盖当前页添加、重复添加、重启恢复、导入、搜索、筛选、详情上下文和 AI 失败。
- 在 Chrome 与 Edge 中完成安装、权限、升级和核心流程验收。
- 检查键盘操作、焦点恢复、可访问名称、reduced-motion 和正文可读性。
- 检查存储使用量并提供可理解的容量状态；只有真实数据证明需要时才申请更高权限。
- 完成 README 的安装、开发、构建、加载扩展、数据位置和隐私说明。
- 用真实数据连续试用，记录添加放弃率、一分钟找回成功率和 AI 修正率。

发布门禁：

- `pnpm check` 全部通过。
- V1 验收场景全部有自动化测试或明确的人工验收记录。
- AI、网络或采集失败不会阻断本地收藏与管理。
- 不包含真实 API Key、浏览数据、正文快照或构建产物。

## 5. 暂不进入排期

以下能力仍遵循 `PRODUCT.md` 的触发条件，在 V1 使用数据证明需求前不进入开发排期：

- 网页变化检测、快照版本历史与差异对比。
- 日报、周刊、邮件或公开发布。
- AI 语义搜索与自然语言需求检索。
- 账户、云同步、移动端和多人协作。
- 项目灵感板、社区推荐、归档、回收站和完整数据导出。
- Redux、虚拟化、服务端抓取或全站 Host Permission。

## 6. 当前迭代建议

下一迭代进入 `v1.0 — Validation Release`，优先完成真实 MV3 端到端测试、Chrome/Edge 验收与无障碍检查。

`v0.3 — Local Core` 已按以下顺序完成：

1. 冻结并补齐领域模型与数据库 schema。
2. 实现 repository、事务去重和数据库迁移测试。
3. 把 Dashboard 查询切换到 Dexie，同时保留现有视觉和交互。
4. 逐个迁移添加、导入、星标和快捷视图写操作。
5. 清除生产路径对 `src/data/demo.ts` 的依赖。
6. 完成重启恢复和数据库升级的人工验收。

在这一阶段结束前，不并行开发 AI、语义搜索、内容简报或云同步。

## 7. 维护规则

- 每个版本开始时，将目标拆成可独立验收的 issue；完成后只更新状态和实际结果，不重写历史。
- 新需求先判断属于 V1 必做范围、后续触发项还是明确非目标，再决定是否进入路线图。
- 改变数据边界、权限模型或扩展交付形态时，先更新或新增 ADR。
- 术语变化同步更新 `CONTEXT.md`；实现细节不得写入领域词汇表。
- 每次发布同时更新本文档的当前版本、阶段状态和质量门禁结果。
